var addToBasketButton = document.getElementById('buy');

addToBasketButton.addEventListener()


function addToBasket (e) {
	var basket = getBasketFromLocalStorage();
	var productName = document.getElementById('product_name').innerHTML;
	
	if (!(basket[productName]) {
		
	}

	if (!quantity) {
		addToBasket.quantity = 0;
	}
	addToBasket.quantity++;
	var purchasedQuantity = addToBasket.quantity;



}